{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "PRISMA_POSTGRES_PRISMA_URL": {
        "description": "Connection string for Prisma to connect to the database"
      },
      "PRISMA_POSTGRES_URL_NON_POOLING": {
        "description": "Connection string for direct database connections"
      }
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
